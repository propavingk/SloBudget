[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "slobudget"
version = "1.0.1"
description = "Error budget accounting for service level objectives."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "SloBudget contributors" }]
keywords = ["sre", "slo", "error-budget", "burn-rate", "observability"]
classifiers = [
